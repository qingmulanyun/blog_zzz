<%= form_for @post, url: user_post_path(@user, @post), method: 'PUT'  do |f|%>
    <%= render 'form', f: f %>
<%end%>

<p class="navbar-text pull-left">
<%= link_to "Cancel", user_post_path(@user, @post), :class => 'btn btn-primary'%>